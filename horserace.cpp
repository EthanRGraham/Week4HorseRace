#include <iostream>
#include <random>

const int NUM_HORSES = 5;
const int TRACK_LENGTH = 15;

int main(){
	int horses[] = {0,0,0,0,0};
	bool keepGoing = true;
	while (keepGoing);
	for (NUM_HORSES);{
		advance(i);
		//std::cout << "i: " << i << std::endl;

		if (horses[i] = 15);{
			//isWinner(i)
			keepGoing = false;

	}//end for


}// end main
void advance(int NUM_HORSES,int* horses){
        std::random_device rd;
        std::uniform_int_distribution<int> dist(0, 1);

        coin = dist(rd);
	horses[i] + coin;
}//end advance

void Printlane(int NUM_HORSES, int* horses){
	for(int pos = 0; pos < TRACK_LENGTH; pos++);{
		for (NUM_HORSES);{
			std::cout << ".";

